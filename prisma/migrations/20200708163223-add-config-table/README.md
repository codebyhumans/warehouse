# Migration `20200708163223-add-config-table`

This migration has been generated by Илья Суходольский at 7/8/2020, 4:32:23 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."Config" (
"key" TEXT NOT NULL  ,"type" TEXT   ,"value" TEXT NOT NULL  )

CREATE UNIQUE INDEX "quaint"."Config.key" ON "Config"("key")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200630192116-first-schema..20200708163223-add-config-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -14,19 +14,25 @@
   roles            Role[]            @relation(references: [id])
   ProductMovements ProductMovement[]
 }
+model Config {
+  key   String  @unique
+  value String
+  type  String?
+}
+
 model Role {
   id    Int    @id @default(autoincrement())
   name  String
   users User[] @relation(references: [id])
 }
 model Unit {
-  id         Int        @id @default(autoincrement())
-  name       String
-  measure    String
-  Products   Product[]
+  id       Int       @id @default(autoincrement())
+  name     String
+  measure  String
+  Products Product[]
 }
 model Provider {
   id               Int               @id @default(autoincrement())
@@ -55,13 +61,13 @@
   ProductMovements ProductMovement[]
 }
 model Category {
-  id         Int        @id @default(autoincrement())
-  name       String
-  order      Int?       @default(0)
-  parentId   Int?
-  Products   Product[]
+  id       Int       @id @default(autoincrement())
+  name     String
+  order    Int?      @default(0)
+  parentId Int?
+  Products Product[]
 }
 model ProductMovement {
   id         Int      @id @default(autoincrement())
```


